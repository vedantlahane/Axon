name: CD Production

on:
  workflow_dispatch:

jobs:
  deploy-prod:
    runs-on: ubuntu-latest
    environment: production
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Production deployment placeholder
        run: |
          echo "Add production deployment commands (e.g., GitHub OIDC, ECS, or SSH-based rollout)"
