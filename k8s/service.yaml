apiVersion: v1
kind: Service
metadata:
  name: axon-backend
  namespace: axon
  labels:
    app: axon
    component: backend
spec:
  type: ClusterIP
  selector:
    app: axon
    component: backend
  ports:
    - name: http
      port: 8000
      targetPort: 8000
      protocol: TCP