# ⚠️  TEMPLATE ONLY — Do NOT commit real values
# Create secrets on cluster using kubectl command below
apiVersion: v1
kind: Secret
metadata:
  name: axon-secrets
  namespace: axon
type: Opaque
stringData:
  SECRET_KEY: "REPLACE_ME"
  DATABASE_URL: "REPLACE_ME"
  GEMINI_API_KEY: ""
  OPENAI_API_KEY: ""